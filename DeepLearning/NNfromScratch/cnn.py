import numpy as np

class ConvNet():
    def __init__(self, X, Y, lr):

def padding(X, pad):
    return X_pad = np.pad(X, ((0,0), (pad, pad), (0, 0)), 'constant',
                          constant_values = (0,0))

